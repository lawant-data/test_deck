source("renv/activate.R")
.First <- function() {
  message("Activating project renv (if present)...")
  if (file.exists("renv/activate.R")) try(source("renv/activate.R"), silent = TRUE)
  options(stringsAsFactors = FALSE, repos = c(CRAN = "https://cloud.r-project.org"))
}

options(digits = 7)

rm(.First)
